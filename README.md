# Self_Pruning_Neural_Network
Differentiable self-pruning neural network using gated weights and lambda scheduling to enable dynamic sparsity during training. Achieves ~99.9% sparsity with ~43% accuracy on CIFAR-10, demonstrating effective model compression while maintaining performance through optimized sparsity–accuracy trade-off.
